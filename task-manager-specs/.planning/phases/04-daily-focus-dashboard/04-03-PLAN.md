---
phase: 04-daily-focus-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - zadachnik-app/prisma/schema.prisma
  - zadachnik-app/scripts/init-db.mjs
  - zadachnik-app/src/lib/dashboard/*
  - zadachnik-app/src/app/api/daily-focus/*
  - zadachnik-app/src/features/dashboard/*
  - zadachnik-app/tests/helpers/access.ts
  - zadachnik-app/tests/dashboard-focus-confirmation.spec.ts
autonomous: true
requirements: [DASH-04, DASH-05, DASH-06]
user_setup: []
must_haves:
  truths:
    - "D-09/D-10/D-11/D-12: Each suggested task is confirmed separately; unconfirmed suggestions remain suggestions; any slot can be manually replaced from the card."
    - "D-05: Confirmation UI keeps the top-3 soft and user-controlled."
    - "D-06/D-07/D-08: Confirmed focus state does not remove the short reason labels or ranking transparency."
  artifacts:
    - path: "zadachnik-app/prisma/schema.prisma"
      provides: "Durable confirmed daily focus slot model"
      contains: "DailyFocusSelection"
    - path: "zadachnik-app/src/app/api/daily-focus/route.ts"
      provides: "Protected confirm/replace API for daily focus slots"
      contains: "requireApiSession"
    - path: "zadachnik-app/tests/dashboard-focus-confirmation.spec.ts"
      provides: "E2E coverage for per-card confirmation, replacement, and unconfirmed suggestion separation"
      contains: "confirmed separately"
  key_links:
    - from: "zadachnik-app/src/features/dashboard/DailyFocusSuggestions.tsx"
      to: "zadachnik-app/src/app/api/daily-focus/route.ts"
      via: "client focus controls confirm or replace one slot at a time"
      pattern: "daily-focus"
---

<objective>
Implement per-task daily focus confirmation and replacement.

Purpose: let the user turn soft top-3 suggestions into confirmed focus tasks one by one, or replace any suggested slot manually.
Output: durable focus-selection state, protected API, interactive Dashboard controls, and confirmation/replacement tests.
</objective>

<execution_context>
@C:/Users/maksd/OneDrive/Документы/New project 2/task-manager-specs/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/OneDrive/Документы/New project 2/task-manager-specs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-daily-focus-dashboard/04-CONTEXT.md
@.planning/phases/04-daily-focus-dashboard/04-RESEARCH.md
@.planning/phases/04-daily-focus-dashboard/04-02-SUMMARY.md
@zadachnik-app/prisma/schema.prisma
@zadachnik-app/scripts/init-db.mjs
@zadachnik-app/src/lib/api-session.ts
@zadachnik-app/src/app/api/tasks/[id]/route.ts
@zadachnik-app/src/features/dashboard/DailyFocusSuggestions.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add confirmed daily focus persistence</name>
  <files>zadachnik-app/prisma/schema.prisma, zadachnik-app/scripts/init-db.mjs, zadachnik-app/tests/helpers/access.ts</files>
  <read_first>.planning/phases/04-daily-focus-dashboard/04-RESEARCH.md, zadachnik-app/prisma/schema.prisma, zadachnik-app/scripts/init-db.mjs, zadachnik-app/tests/helpers/access.ts</read_first>
  <action>Add a small `DailyFocusSelection` persistence model with focus date, slot, task relation, confirmation timestamp, and timestamps. Update the custom SQLite init path and test cleanup helpers. This table stores confirmed focus tasks only; it must not store unconfirmed suggestions.</action>
  <verify>`cd zadachnik-app && npm run db:generate && npm run typecheck`</verify>
  <acceptance_criteria>
    - Confirmed focus selection can store one task per date/slot.
    - Deleting/archiving a task cannot leave a hard-blocking orphan relation.
    - Test reset clears focus selections.
    - Unconfirmed suggestions are not persisted.
  </acceptance_criteria>
  <done>Daily focus confirmation state is durable and test-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Add protected daily focus API</name>
  <files>zadachnik-app/src/app/api/daily-focus/route.ts, zadachnik-app/src/lib/dashboard/focus-selection.ts</files>
  <read_first>zadachnik-app/src/lib/api-session.ts, zadachnik-app/src/app/api/tasks/[id]/route.ts, zadachnik-app/src/lib/dashboard/focus-ranking.ts</read_first>
  <action>Add protected API support to confirm a specific suggested task into a slot and replace a slot with a manually selected open task. Validate slot indexes, task existence, open status, and date. Return updated confirmed selections and keep API errors consistent with existing task routes.</action>
  <verify>`cd zadachnik-app && npm run typecheck`</verify>
  <acceptance_criteria>
    - Unauthorized calls are rejected through existing session helper.
    - Confirming one slot does not confirm other slots.
    - Replacing a slot updates only that slot.
    - Done/archived tasks cannot be selected.
  </acceptance_criteria>
  <done>Daily focus confirm/replace API is available.</done>
</task>

<task type="auto">
  <name>Task 3: Add per-card confirmation and replacement UI</name>
  <files>zadachnik-app/src/features/dashboard/DailyFocusSuggestions.tsx, zadachnik-app/src/features/dashboard/FocusSlotControls.tsx, zadachnik-app/src/features/dashboard/DashboardHome.tsx, zadachnik-app/src/app/globals.css</files>
  <read_first>.planning/phases/04-daily-focus-dashboard/04-CONTEXT.md, zadachnik-app/src/features/dashboard/DailyFocusSuggestions.tsx, zadachnik-app/src/features/tasks/TaskCard.tsx</read_first>
  <action>Add touch-friendly controls on each focus slot. `Подтвердить` confirms only that task. `Заменить` opens a compact replacement list of eligible open tasks and writes the chosen task into that slot. Confirmed slots should look selected; unconfirmed slots should still look like suggestions.</action>
  <verify>`cd zadachnik-app && npm run typecheck`</verify>
  <acceptance_criteria>
    - Each of three slots can be confirmed independently.
    - Replacement is available from each slot.
    - Unconfirmed suggestions remain visually and semantically distinct from confirmed focus tasks.
    - Controls remain usable on mobile.
  </acceptance_criteria>
  <done>Dashboard supports per-task focus confirmation and replacement.</done>
</task>

<task type="auto">
  <name>Task 4: Cover focus confirmation and replacement</name>
  <files>zadachnik-app/tests/dashboard-focus-confirmation.spec.ts</files>
  <read_first>zadachnik-app/tests/dashboard-focus-ranking.spec.ts, zadachnik-app/tests/helpers/access.ts</read_first>
  <action>Add Playwright tests proving that confirming one top-3 suggestion does not confirm the others, unconfirmed suggestions remain suggestions, replacing a slot works, and persisted confirmed focus state survives a page reload.</action>
  <verify>`cd zadachnik-app && npm run test:e2e -- dashboard-focus-confirmation.spec.ts dashboard-focus-ranking.spec.ts`</verify>
  <acceptance_criteria>
    - Tests cover DASH-04, DASH-05, and DASH-06.
    - Tests verify persistence after reload.
    - Tests verify replacement does not duplicate the same task across slots where avoidable.
  </acceptance_criteria>
  <done>Focus confirmation and replacement are covered by E2E tests.</done>
</task>

</tasks>

<verification>
- [ ] `cd zadachnik-app && npm run db:generate`
- [ ] `cd zadachnik-app && npm run typecheck`
- [ ] `cd zadachnik-app && npm run test:e2e -- dashboard-focus-confirmation.spec.ts dashboard-focus-ranking.spec.ts`
</verification>

<success_criteria>
- User can confirm each proposed top-3 task separately.
- User can replace any proposed main task.
- Unconfirmed proposed tasks remain suggestions and are not selected.
- Confirmed focus state persists across reloads.
</success_criteria>

<output>
After completion, create `.planning/phases/04-daily-focus-dashboard/04-03-SUMMARY.md`.
</output>
