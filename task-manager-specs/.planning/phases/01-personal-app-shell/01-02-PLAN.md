---
phase: 01-personal-app-shell
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - zadachnik-app/prisma/schema.prisma
  - zadachnik-app/src/lib/access-tokens.ts
  - zadachnik-app/src/lib/session.ts
  - zadachnik-app/src/middleware.ts
  - zadachnik-app/src/app/a/[token]/route.ts
  - zadachnik-app/src/app/api/access/regenerate/route.ts
  - zadachnik-app/src/app/api/access/current/route.ts
  - zadachnik-app/src/app/more/access/page.tsx
  - zadachnik-app/tests/access.spec.ts
autonomous: true
requirements: [ACCS-02, ACCS-03]
user_setup: []
must_haves:
  truths:
    - "D-04/D-06: User can enter the MVP through a private secret link without creating an account or using heavy authentication."
    - "D-05: User can regenerate the private access link and the previous link stops working."
    - "ACCS-02/ACCS-03: Private access and link regeneration are backed by persistent data."
  artifacts:
    - path: "zadachnik-app/src/lib/access-tokens.ts"
      provides: "Token creation, hashing, validation, and rotation"
      contains: "regenerate"
    - path: "zadachnik-app/src/app/a/[token]/route.ts"
      provides: "Private link entry route"
      contains: "token"
    - path: "zadachnik-app/src/app/api/access/regenerate/route.ts"
      provides: "Regeneration endpoint"
      exports: ["POST"]
    - path: "zadachnik-app/tests/access.spec.ts"
      provides: "Access link smoke tests"
      contains: "regenerate"
  key_links:
    - from: "zadachnik-app/src/app/a/[token]/route.ts"
      to: "zadachnik-app/src/lib/access-tokens.ts"
      via: "token validation"
      pattern: "validate.*Token|access"
    - from: "zadachnik-app/src/app/api/access/regenerate/route.ts"
      to: "zadachnik-app/prisma/schema.prisma"
      via: "AccessToken rotation write"
      pattern: "AccessToken|accessToken"
---

<objective>
Implement the private-link MVP access contract.

Purpose: Make the personal app private enough for MVP while preserving the user's low-friction decision: secret link with regeneration, no heavy auth.
Output: Token utilities, entry route, session guard, regeneration endpoint, More-page access control, and smoke tests.
</objective>

<execution_context>
@C:/Users/maksd/OneDrive/Документы/New project 2/task-manager-specs/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/OneDrive/Документы/New project 2/task-manager-specs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-personal-app-shell/01-CONTEXT.md
@.planning/phases/01-personal-app-shell/01-RESEARCH.md
@.planning/phases/01-personal-app-shell/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement token hashing and validation</name>
  <files>zadachnik-app/src/lib/access-tokens.ts, zadachnik-app/src/lib/session.ts, zadachnik-app/prisma/schema.prisma</files>
  <read_first>zadachnik-app/prisma/schema.prisma, zadachnik-app/src/lib/db.ts, zadachnik-app/src/lib/env.ts, .planning/phases/01-personal-app-shell/01-CONTEXT.md</read_first>
  <action>Implement high-entropy token generation using Node crypto, hash tokens with `ACCESS_TOKEN_PEPPER`, store only `tokenHash`, and expose functions `createInitialAccessToken`, `validateAccessToken`, `regenerateAccessToken`, and `createAccessSessionCookie`. If schema changes are needed, add them through Prisma migration rather than ad hoc SQL. Do not add users, passwords, OAuth, email login, or multi-user concepts.</action>
  <verify>`cd zadachnik-app && npm run typecheck && npx prisma validate`</verify>
  <acceptance_criteria>
    - Raw token values are never stored in the database schema.
    - `regenerateAccessToken` deactivates or rotates the previous active token.
    - Session helper sets an httpOnly cookie after a valid private link.
  </acceptance_criteria>
  <done>Token lifecycle works through persistent storage and keeps the MVP auth lightweight.</done>
</task>

<task type="auto">
  <name>Task 2: Add private entry route and route guard</name>
  <files>zadachnik-app/src/app/a/[token]/route.ts, zadachnik-app/src/middleware.ts, zadachnik-app/src/app/api/access/current/route.ts</files>
  <read_first>zadachnik-app/src/lib/access-tokens.ts, zadachnik-app/src/lib/session.ts</read_first>
  <action>Create `/a/[token]` route that validates the token, sets the session cookie, records a `ShellEvent` of kind `access.validated`, and redirects to Dashboard. Add middleware protecting app routes so unauthenticated access goes to a simple private-link-required state. Add `/api/access/current` returning whether the current request has a valid session.</action>
  <verify>`cd zadachnik-app && npm run typecheck && npm run build`</verify>
  <acceptance_criteria>
    - `/a/[token]` validates token before session creation.
    - Protected app routes cannot be opened without a valid token/session.
    - Valid private link redirects to Dashboard.
  </acceptance_criteria>
  <done>Private link grants access and unvalidated users cannot open the shell.</done>
</task>

<task type="auto">
  <name>Task 3: Add regeneration UI and tests</name>
  <files>zadachnik-app/src/app/api/access/regenerate/route.ts, zadachnik-app/src/app/more/access/page.tsx, zadachnik-app/tests/access.spec.ts</files>
  <read_first>zadachnik-app/src/lib/access-tokens.ts, zadachnik-app/src/app/a/[token]/route.ts</read_first>
  <action>Add authenticated `POST /api/access/regenerate` route that rotates the token and returns the new private URL. Add Russian UI under `Еще -> Доступ` with a button labelled `Перегенерировать ссылку`, confirmation copy, and a copyable resulting URL. Add Playwright tests covering valid link entry, regeneration, and old-link rejection. Keep the UI personal and minimal.</action>
  <verify>`cd zadachnik-app && npm run test:e2e -- access.spec.ts`</verify>
  <acceptance_criteria>
    - Regeneration button is available only after private access.
    - New URL is displayed after regeneration.
    - Old token no longer grants access in the test.
  </acceptance_criteria>
  <done>User can regenerate the private link from the app and rely on old-link invalidation.</done>
</task>

</tasks>

<verification>
- [ ] `cd zadachnik-app && npm run typecheck`
- [ ] `cd zadachnik-app && npm run build`
- [ ] `cd zadachnik-app && npm run test:e2e -- access.spec.ts`
</verification>

<success_criteria>
- MVP private link access works without accounts.
- Regeneration invalidates the previous token.
- Persistent access token state exists in the database.
- Requirements `ACCS-02` and `ACCS-03` are covered.
</success_criteria>

<output>
After completion, create `.planning/phases/01-personal-app-shell/01-02-SUMMARY.md`.
</output>
