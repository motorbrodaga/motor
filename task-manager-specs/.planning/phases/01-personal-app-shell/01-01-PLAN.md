---
phase: 01-personal-app-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zadachnik-app/package.json
  - zadachnik-app/next.config.ts
  - zadachnik-app/tsconfig.json
  - zadachnik-app/postcss.config.mjs
  - zadachnik-app/eslint.config.mjs
  - zadachnik-app/src/app/layout.tsx
  - zadachnik-app/src/app/page.tsx
  - zadachnik-app/src/app/globals.css
  - zadachnik-app/src/app/manifest.ts
  - zadachnik-app/src/lib/env.ts
  - zadachnik-app/src/lib/db.ts
  - zadachnik-app/prisma/schema.prisma
  - zadachnik-app/prisma/seed.ts
  - zadachnik-app/.env.example
  - zadachnik-app/README.md
autonomous: true
requirements: [ACCS-01, MOBL-01]
user_setup: []
must_haves:
  truths:
    - "D-01/D-02: User can start one PWA/mobile-first web app entry point from the new app folder."
    - "D-11/D-12: The app has a real persistence foundation with a migrated database, not in-memory placeholder storage."
    - "ACCS-01: Phone and desktop can point at the same running server and therefore the same database."
  artifacts:
    - path: "zadachnik-app/package.json"
      provides: "Next.js app scripts and dependencies"
      contains: "next"
    - path: "zadachnik-app/prisma/schema.prisma"
      provides: "Initial persistent schema for shell/access foundation"
      contains: "datasource db"
    - path: "zadachnik-app/src/lib/db.ts"
      provides: "Shared database client"
      contains: "PrismaClient"
    - path: "zadachnik-app/src/app/manifest.ts"
      provides: "PWA manifest metadata"
      contains: "Задачник"
  key_links:
    - from: "zadachnik-app/src/lib/db.ts"
      to: "zadachnik-app/prisma/schema.prisma"
      via: "Prisma generated client"
      pattern: "PrismaClient"
---

<objective>
Create the Phase 1 walking skeleton foundation for Задачник.

Purpose: Establish the actual product app separately from the specs and nested GSD tooling, with a real PWA-capable Next.js shell and real database foundation.
Output: `zadachnik-app/` scaffold, Prisma SQLite schema, seed path, PWA manifest, and local run documentation.
</objective>

<execution_context>
@C:/Users/maksd/OneDrive/Документы/New project 2/task-manager-specs/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/OneDrive/Документы/New project 2/task-manager-specs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-personal-app-shell/01-CONTEXT.md
@.planning/phases/01-personal-app-shell/01-RESEARCH.md
@.planning/phases/01-personal-app-shell/01-SKELETON.md
@05-tech-decisions.md
@07-development-brief.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the product app</name>
  <files>zadachnik-app/package.json, zadachnik-app/next.config.ts, zadachnik-app/tsconfig.json, zadachnik-app/postcss.config.mjs, zadachnik-app/eslint.config.mjs, zadachnik-app/src/app/layout.tsx, zadachnik-app/src/app/page.tsx, zadachnik-app/src/app/globals.css, zadachnik-app/src/app/manifest.ts</files>
  <read_first>.planning/phases/01-personal-app-shell/01-CONTEXT.md, .planning/phases/01-personal-app-shell/01-RESEARCH.md</read_first>
  <action>Create a new `zadachnik-app/` Next.js App Router TypeScript app skeleton. Use Tailwind CSS. Add scripts: `dev`, `dev:lan` binding host `0.0.0.0`, `build`, `lint`, `typecheck`, `test:e2e`. Add a root page that redirects or links into the private access flow later; do not implement task CRUD. Add `src/app/manifest.ts` with Russian app name `Задачник`, short name `Задачник`, display `standalone`, and mobile app colors. Keep all visible placeholder copy Russian-only where present.</action>
  <verify>From `zadachnik-app/`, run `npm install`, `npm run typecheck`, and `npm run build`.</verify>
  <acceptance_criteria>
    - `zadachnik-app/package.json` contains `next`, `react`, `typescript`, Tailwind tooling, Prisma tooling, and Playwright dev dependency.
    - `zadachnik-app/src/app/manifest.ts` contains `Задачник` and `standalone`.
    - `zadachnik-app/src/app/layout.tsx` sets Russian metadata and `lang="ru"` through the HTML element.
  </acceptance_criteria>
  <done>App scaffold builds successfully and exposes PWA metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Add Prisma SQLite persistence foundation</name>
  <files>zadachnik-app/prisma/schema.prisma, zadachnik-app/prisma/seed.ts, zadachnik-app/src/lib/db.ts, zadachnik-app/src/lib/env.ts, zadachnik-app/.env.example</files>
  <read_first>.planning/phases/01-personal-app-shell/01-CONTEXT.md, .planning/phases/01-personal-app-shell/01-SKELETON.md</read_first>
  <action>Add Prisma with SQLite datasource `file:./dev.db`. Define minimal shell foundation models: `AccessToken` with `id`, `tokenHash`, `label`, `active`, `createdAt`, `rotatedAt`; `AppSetting` with `key`, `value`, `updatedAt`; and `ShellEvent` with `id`, `kind`, `createdAt`, `metadataJson` for simple proof of writes. Add `src/lib/db.ts` exporting a singleton Prisma client. Add `src/lib/env.ts` for required env reads. Add `.env.example` with `DATABASE_URL="file:./dev.db"` and a placeholder `ACCESS_TOKEN_PEPPER` value, but no real secrets.</action>
  <verify>From `zadachnik-app/`, run `npx prisma validate`, `npx prisma migrate dev --name init`, `npx prisma db seed`, and `npm run typecheck`.</verify>
  <acceptance_criteria>
    - `schema.prisma` defines `AccessToken`, `AppSetting`, and `ShellEvent`.
    - `.env.example` contains `DATABASE_URL` and `ACCESS_TOKEN_PEPPER` only as safe placeholders.
    - `src/lib/db.ts` exports the shared Prisma client without leaking secrets.
  </acceptance_criteria>
  <done>Database schema validates, migrates, and seeds without storing personal secrets in the repo.</done>
</task>

<task type="auto">
  <name>Task 3: Document local phone and desktop run path</name>
  <files>zadachnik-app/README.md</files>
  <read_first>.planning/phases/01-personal-app-shell/01-RESEARCH.md, AGENTS.md</read_first>
  <action>Write a concise Russian README for Phase 1 local use: install dependencies, copy `.env.example` to `.env`, run migrations/seed, start `npm run dev` for desktop or `npm run dev:lan` for phone on the same Wi-Fi. Mention that full offline/backups are deferred and that `.env` must not be committed. Do not include Telegram session details or real secrets.</action>
  <verify>Review README and run `git diff -- zadachnik-app/README.md` to confirm no secrets are included.</verify>
  <acceptance_criteria>
    - README describes one app entry point for desktop and phone.
    - README explicitly warns that `.env` is local-only.
    - README does not mention or copy `.telegram_sessions` contents.
  </acceptance_criteria>
  <done>Developer can run the skeleton locally and open it from phone/desktop against one server/database.</done>
</task>

</tasks>

<verification>
- [ ] `cd zadachnik-app && npm run typecheck`
- [ ] `cd zadachnik-app && npm run build`
- [ ] `cd zadachnik-app && npx prisma validate`
- [ ] `cd zadachnik-app && npx prisma migrate dev --name init`
</verification>

<success_criteria>
- The app is scaffolded under `zadachnik-app/`, not mixed into `gsd-get-shit-done/`.
- PWA metadata exists.
- Real Prisma SQLite persistence exists.
- Local phone/desktop run path is documented.
- Requirements `ACCS-01` and `MOBL-01` have foundation coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/01-personal-app-shell/01-01-SUMMARY.md`.
</output>
