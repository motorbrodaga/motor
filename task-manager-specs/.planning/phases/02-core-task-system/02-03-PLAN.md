---
phase: 02-core-task-system
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - zadachnik-app/src/app/api/tasks/[id]/route.ts
  - zadachnik-app/src/features/tasks/TaskForm.tsx
  - zadachnik-app/src/features/tasks/TaskCard.tsx
  - zadachnik-app/src/features/tasks/task-formatters.ts
  - zadachnik-app/src/app/(app)/dashboard/page.tsx
  - zadachnik-app/src/features/dashboard/DashboardHome.tsx
  - zadachnik-app/tests/task-fields.spec.ts
autonomous: true
requirements: [TASK-05, TASK-06, TASK-07, TASK-08, TASK-09, TASK-10, TASK-11, TASK-12, TASK-14]
user_setup: []
must_haves:
  truths:
    - "D-08: dueDate is the deadline and doDate is the planned work day; labels must keep them distinct."
    - "D-09: importance is normal/important and separate from binary isUrgent."
    - "D-10: estimatedMinutes and actualMinutes are manual numeric values; no start/stop timer is added."
    - "D-18/D-23/D-25: Task cards and Dashboard summaries show Phase 2 metadata in Russian without Phase 4 ranking."
  artifacts:
    - path: "zadachnik-app/src/features/tasks/TaskForm.tsx"
      provides: "Mobile-first task metadata editing"
      contains: "День выполнения"
    - path: "zadachnik-app/src/features/tasks/TaskCard.tsx"
      provides: "Scan-friendly metadata signals"
      contains: "Срочно"
    - path: "zadachnik-app/src/features/tasks/task-formatters.ts"
      provides: "Shared Russian date/minute/status formatting"
      contains: "doDate"
  key_links:
    - from: "zadachnik-app/src/features/tasks/TaskForm.tsx"
      to: "zadachnik-app/src/app/api/tasks/[id]/route.ts"
      via: "PATCH metadata updates"
      pattern: "dueDate|doDate|estimatedMinutes|actualMinutes"
---

<objective>
Implement task scheduling fields, importance, urgency, time estimates, actual time, and description editing.

Purpose: make the task object useful for planning without implementing later ranking, reviews, calendar export, or timers.
Output: task form controls, API validation, task card signals, Dashboard-safe summaries, and E2E tests for all metadata fields.
</objective>

<execution_context>
@C:/Users/maksd/OneDrive/Документы/New project 2/task-manager-specs/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/OneDrive/Документы/New project 2/task-manager-specs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-task-system/02-CONTEXT.md
@.planning/phases/02-core-task-system/02-RESEARCH.md
@.planning/phases/02-core-task-system/02-UI-SPEC.md
@.planning/phases/02-core-task-system/02-01-SUMMARY.md
@.planning/phases/02-core-task-system/02-02-SUMMARY.md
@zadachnik-app/src/features/tasks/TaskForm.tsx
@zadachnik-app/src/features/tasks/TaskCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend task update validation and formatting</name>
  <files>zadachnik-app/src/app/api/tasks/[id]/route.ts, zadachnik-app/src/lib/tasks/task-validation.ts, zadachnik-app/src/features/tasks/task-formatters.ts</files>
  <read_first>zadachnik-app/src/lib/tasks/task-options.ts, .planning/phases/02-core-task-system/02-CONTEXT.md</read_first>
  <action>Support PATCH updates for status, dueDate, doDate, importance, isUrgent, estimatedMinutes, actualMinutes, and description. Normalize empty values to null where appropriate. Add formatting helpers for Russian status labels, date labels, and minute labels used by cards and forms.</action>
  <verify>`cd zadachnik-app && npm run typecheck`</verify>
  <acceptance_criteria>
    - Empty optional date/time fields can be saved.
    - Invalid status/importance/minute values are rejected.
    - Formatter helpers do not implement ranking logic.
  </acceptance_criteria>
  <done>Task metadata can be safely updated through the API.</done>
</task>

<task type="auto">
  <name>Task 2: Add mobile-first metadata controls to task form</name>
  <files>zadachnik-app/src/features/tasks/TaskForm.tsx, zadachnik-app/src/app/(app)/tasks/[id]/page.tsx</files>
  <read_first>.planning/phases/02-core-task-system/02-UI-SPEC.md</read_first>
  <action>Add form controls for status, do date, due date, importance, urgency, estimated minutes, actual minutes, and description. Keep title/save/status prominent and group advanced fields below as UI-SPEC requires. Use segmented controls where practical and native date/number inputs for dates/minutes.</action>
  <verify>`cd zadachnik-app && npm run typecheck && npm run build`</verify>
  <acceptance_criteria>
    - `День выполнения` and `Крайний срок` are visibly separate.
    - `Важность` and `Срочно` are separate controls.
    - No start/stop timer is present.
    - Form remains usable on mobile without horizontal scroll.
  </acceptance_criteria>
  <done>User can edit all Phase 2 metadata fields.</done>
</task>

<task type="auto">
  <name>Task 3: Show metadata signals on task cards and Dashboard preview</name>
  <files>zadachnik-app/src/features/tasks/TaskCard.tsx, zadachnik-app/src/features/dashboard/DashboardHome.tsx, zadachnik-app/src/app/(app)/dashboard/page.tsx</files>
  <read_first>.planning/phases/02-core-task-system/02-UI-SPEC.md, .planning/phases/02-core-task-system/02-CONTEXT.md</read_first>
  <action>Update task cards to display status, do/due date signals, important and urgent signals, and time estimates where present without visual overload. Update Dashboard to show simple real counts/previews such as open, planned today, overdue, and important counts if easy from Phase 2 data; do not implement top-3 suggestions, confirmation, ranking weights, or morning review behavior.</action>
  <verify>`cd zadachnik-app && npm run typecheck && npm run build`</verify>
  <acceptance_criteria>
    - Cards are scan-friendly on phone.
    - Color is not the only signal for urgency or status.
    - Dashboard remains Phase 2 summary only.
  </acceptance_criteria>
  <done>Task metadata is visible where it helps scanning.</done>
</task>

<task type="auto">
  <name>Task 4: Cover metadata editing with E2E tests</name>
  <files>zadachnik-app/tests/task-fields.spec.ts</files>
  <read_first>zadachnik-app/tests/task-crud.spec.ts, zadachnik-app/tests/helpers/access.ts</read_first>
  <action>Add Playwright tests for editing due date, do date, importance, urgency, estimated minutes, actual minutes, status, and description. Assert the saved values appear on the task card/details and that empty optional fields are allowed.</action>
  <verify>`cd zadachnik-app && npm run test:e2e`</verify>
  <acceptance_criteria>
    - Tests prove do date and due date are distinct.
    - Tests prove urgency and importance are distinct.
    - Tests prove manual time fields save without a timer.
  </acceptance_criteria>
  <done>Task metadata workflows are verified end to end.</done>
</task>

</tasks>

<verification>
- [ ] `cd zadachnik-app && npm run typecheck`
- [ ] `cd zadachnik-app && npm run build`
- [ ] `cd zadachnik-app && npm run test:e2e`
</verification>

<success_criteria>
- User can set status, importance, urgency, due date, do date, estimated time, actual time, and description.
- Task cards show useful metadata without overload.
- Dashboard shows only Phase 2-safe real summaries.
- Later Phase 4 ranking/review scope remains deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-task-system/02-03-SUMMARY.md`.
</output>

