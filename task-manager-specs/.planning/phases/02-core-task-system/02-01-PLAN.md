---
phase: 02-core-task-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zadachnik-app/prisma/schema.prisma
  - zadachnik-app/prisma/migrations/*/migration.sql
  - zadachnik-app/prisma/seed.ts
  - zadachnik-app/scripts/init-db.mjs
  - zadachnik-app/src/lib/tasks/task-options.ts
  - zadachnik-app/src/lib/tasks/task-validation.ts
  - zadachnik-app/tests/helpers/access.ts
autonomous: true
requirements: [TASK-01, TASK-02, TASK-03, TASK-04, TASK-05, TASK-06, TASK-07, TASK-08, TASK-09, TASK-10, TASK-11, TASK-12, TASK-13, TASK-14, TASK-15, TASK-16, ORGN-01, ORGN-02, ORGN-03, ORGN-04, ORGN-05, ORGN-06, ORGN-07]
user_setup: []
must_haves:
  truths:
    - "D-01/D-03/D-04/D-05: Task stores inbox, todo, in_progress, waiting, done, completedAt, and archivedAt; blocked is not introduced."
    - "D-08/D-09/D-10/D-11: Task schema separates dueDate, doDate, importance, urgency, estimated minutes, actual minutes, description, and note feed."
    - "D-13/D-14/D-15/D-16/D-17: Category, Context, TaskContext, Project, and TaskNote are relational; tags are not added."
    - "D-19: Person assignment is scoped as a lightweight nullable task field or neutral minimal reference without Phase 5 waiting behavior."
    - "D-26/D-27/D-29/D-30: Extend Prisma/SQLite and the custom migration/init path; real tasks do not use ShellEvent."
  artifacts:
    - path: "zadachnik-app/prisma/schema.prisma"
      provides: "Core task and organization relational schema"
      contains: "model Task"
    - path: "zadachnik-app/scripts/init-db.mjs"
      provides: "Custom SQLite migration/init path for Phase 2 schema"
      contains: "migration.sql"
    - path: "zadachnik-app/prisma/seed.ts"
      provides: "Default categories and contexts"
      contains: "Работа"
    - path: "zadachnik-app/src/lib/tasks/task-options.ts"
      provides: "Shared task status, importance, category color, and context constants"
      contains: "inbox"
  key_links:
    - from: "zadachnik-app/src/lib/tasks/task-options.ts"
      to: "zadachnik-app/prisma/schema.prisma"
      via: "shared enum-like constants mirror persisted task values"
      pattern: "inbox|important|dueDate|doDate"
---

<objective>
Define the Phase 2 data foundation for real tasks, organization, notes, and custom SQLite setup.

Purpose: replace Phase 1 placeholder capture storage with a durable relational model that all later Phase 2 UI and API work can use.
Output: Prisma schema, SQL migration, seed/default data path, shared task constants/validation helpers, and test helpers prepared for task data cleanup.
</objective>

<execution_context>
@C:/Users/maksd/OneDrive/Документы/New project 2/task-manager-specs/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/OneDrive/Документы/New project 2/task-manager-specs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-task-system/02-CONTEXT.md
@.planning/phases/02-core-task-system/02-RESEARCH.md
@.planning/phases/02-core-task-system/02-UI-SPEC.md
@zadachnik-app/prisma/schema.prisma
@zadachnik-app/scripts/init-db.mjs
@zadachnik-app/prisma/seed.ts
@zadachnik-app/tests/helpers/access.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Prisma schema with core task models</name>
  <files>zadachnik-app/prisma/schema.prisma</files>
  <read_first>.planning/phases/02-core-task-system/02-CONTEXT.md, .planning/phases/02-core-task-system/02-RESEARCH.md</read_first>
  <action>Add relational models for Task, Category, Context, TaskContext, Project, and TaskNote. Include status values as strings constrained through application validation: `inbox`, `todo`, `in_progress`, `waiting`, `done`. Add fields for title, description, importance, isUrgent, dueDate, doDate, estimatedMinutes, actualMinutes, categoryId, projectId, personLabel or equivalent lightweight person assignment, completedAt, archivedAt, createdAt, and updatedAt. Preserve AccessToken, AppSetting, and ShellEvent unchanged.</action>
  <verify>`cd zadachnik-app && npx prisma validate`</verify>
  <acceptance_criteria>
    - `Task` contains all Phase 2 persisted task fields.
    - `TaskNote` stores append-only note bodies with timestamps.
    - Contexts are many-to-many through `TaskContext`.
    - No tags, subtasks, attachments, recurrence, waiting direction, or timer model is introduced.
  </acceptance_criteria>
  <done>Prisma schema validates with core Phase 2 models.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic SQLite migration and init support</name>
  <files>zadachnik-app/prisma/migrations/*/migration.sql, zadachnik-app/scripts/init-db.mjs</files>
  <read_first>zadachnik-app/scripts/init-db.mjs, .planning/phases/01-personal-app-shell/01-01-SUMMARY.md</read_first>
  <action>Add a Phase 2 SQL migration for the new task and organization tables. Update the custom init script so it applies ordered migrations reliably on a fresh database in this Windows/OneDrive workspace. Keep the script safe for existing Phase 1 data and avoid dropping private access tables.</action>
  <verify>`cd zadachnik-app && npm run db:migrate`</verify>
  <acceptance_criteria>
    - Fresh database init creates all task tables.
    - Existing `AccessToken`, `AppSetting`, and `ShellEvent` data paths remain intact.
    - Migration order is deterministic.
  </acceptance_criteria>
  <done>Custom SQLite init creates the Phase 2 schema.</done>
</task>

<task type="auto">
  <name>Task 3: Seed defaults and shared task constants</name>
  <files>zadachnik-app/prisma/seed.ts, zadachnik-app/src/lib/tasks/task-options.ts, zadachnik-app/src/lib/tasks/task-validation.ts</files>
  <read_first>.planning/phases/02-core-task-system/02-UI-SPEC.md, .planning/REQUIREMENTS.md</read_first>
  <action>Seed default categories `Работа`, `Личное`, `Звонки`, `Дом` with UI-SPEC colors, and default contexts `Звонок`, `Компьютер`, `Дом`, `В дороге`, `С человеком`. Add shared constants and validation helpers for task statuses, importance values, dates, minutes, and optional fields. Keep validation tolerant of empty optional fields while requiring title for manual creation.</action>
  <verify>`cd zadachnik-app && npm run db:seed && npm run typecheck`</verify>
  <acceptance_criteria>
    - Default categories and contexts are created if missing.
    - Re-running seed does not duplicate defaults.
    - Validation requires title only for quick creation.
  </acceptance_criteria>
  <done>Defaults and validation are reusable by APIs and tests.</done>
</task>

<task type="auto">
  <name>Task 4: Prepare E2E helpers for real task data</name>
  <files>zadachnik-app/tests/helpers/access.ts</files>
  <read_first>zadachnik-app/tests/helpers/access.ts, zadachnik-app/prisma/schema.prisma</read_first>
  <action>Extend Playwright helpers with safe task data cleanup and default organization setup helpers. Ensure cleanup removes task-related rows in dependency order without touching access tokens unless a test explicitly requests access setup.</action>
  <verify>`cd zadachnik-app && npm run typecheck`</verify>
  <acceptance_criteria>
    - Tests can start from a clean task state.
    - Private-link helpers still work.
    - Cleanup does not delete unrelated user secrets or Telegram files.
  </acceptance_criteria>
  <done>Test helpers can support Phase 2 workflow tests.</done>
</task>

</tasks>

<verification>
- [ ] `cd zadachnik-app && npx prisma validate`
- [ ] `cd zadachnik-app && npm run db:generate`
- [ ] `cd zadachnik-app && npm run db:migrate`
- [ ] `cd zadachnik-app && npm run db:seed`
- [ ] `cd zadachnik-app && npm run typecheck`
</verification>

<success_criteria>
- Core task data is stored in relational Prisma/SQLite tables.
- Default categories and contexts exist and are idempotent.
- ShellEvent remains shell telemetry only.
- Phase 2 schema does not pull in deferred tags, recurrence, subtasks, attachments, timers, or full waiting workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-task-system/02-01-SUMMARY.md`.
</output>

