---
phase: 03-assistant-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zadachnik-app/src/lib/assistant-capture/*
  - zadachnik-app/src/app/api/assistant-capture/interpret/route.ts
  - zadachnik-app/src/lib/tasks/task-validation.ts
  - zadachnik-app/tests/assistant-capture-parser.spec.ts
autonomous: true
requirements: [CAPT-01, CAPT-02, CAPT-04, CAPT-05]
user_setup: []
must_haves:
  truths:
    - "D-04/D-05/D-06: Interpreter returns at most one or two clarification questions and focuses on date, category, and importance."
    - "D-12/D-13/D-14/D-15/D-16: Interpreter recognizes title, date, category, and importance when reasonably obvious; unknown fields remain empty."
    - "D-13: Natural date parsing preserves the distinction between doDate and dueDate."
    - "D-14: Category matching uses active existing categories and does not invent new categories."
    - "D-17/D-19: Interpretation endpoint is protected by the existing private-link session boundary and prepares data for the existing task model."
  artifacts:
    - path: "zadachnik-app/src/lib/assistant-capture/interpret-task-capture.ts"
      provides: "Pure assistant-capture interpretation contract and parser"
      contains: "interpretTaskCapture"
    - path: "zadachnik-app/src/app/api/assistant-capture/interpret/route.ts"
      provides: "Protected interpretation API for the quick capture panel"
      contains: "requireApiSession"
    - path: "zadachnik-app/tests/assistant-capture-parser.spec.ts"
      provides: "Parser coverage for date, category, importance, ambiguity, and incomplete data"
      contains: "assistant capture"
  key_links:
    - from: "zadachnik-app/src/app/api/assistant-capture/interpret/route.ts"
      to: "zadachnik-app/src/lib/assistant-capture/interpret-task-capture.ts"
      via: "route delegates interpretation to pure parser with active organization options"
      pattern: "interpretTaskCapture"
---

<objective>
Define and implement the assistant-capture interpretation contract for Phase 3.

Purpose: turn a user's quick natural-language phrase into a safe staged task interpretation without saving it.
Output: a protected interpretation endpoint, pure parser module, shared types, and parser tests for title, date, category, and importance.
</objective>

<execution_context>
@C:/Users/maksd/OneDrive/Документы/New project 2/task-manager-specs/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/OneDrive/Документы/New project 2/task-manager-specs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-assistant-capture/03-CONTEXT.md
@.planning/phases/03-assistant-capture/03-RESEARCH.md
@.planning/phases/02-core-task-system/02-CONTEXT.md
@zadachnik-app/src/lib/tasks/task-options.ts
@zadachnik-app/src/lib/tasks/task-validation.ts
@zadachnik-app/src/lib/tasks/task-queries.ts
@zadachnik-app/src/app/api/tasks/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add assistant-capture types and interpretation module</name>
  <files>zadachnik-app/src/lib/assistant-capture/*</files>
  <read_first>.planning/phases/03-assistant-capture/03-CONTEXT.md, .planning/phases/03-assistant-capture/03-RESEARCH.md, zadachnik-app/src/lib/tasks/task-options.ts</read_first>
  <action>Create a small assistant-capture library with a typed staged interpretation result. Include fields for original phrase, title, doDate, dueDate, categoryId/categoryName, importance, confidence or warnings, missing optional fields, and up to two clarification questions. Keep the parser deterministic and pure enough to unit test.</action>
  <verify>`cd zadachnik-app && npm run typecheck`</verify>
  <acceptance_criteria>
    - The interpreter does not create or mutate tasks.
    - The interpreter returns no more than two clarification questions.
    - The result can represent incomplete task data safely.
    - No external AI provider or secrets are introduced.
  </acceptance_criteria>
  <done>Assistant-capture interpretation types and parser compile.</done>
</task>

<task type="auto">
  <name>Task 2: Parse Phase 3 date, category, and importance signals</name>
  <files>zadachnik-app/src/lib/assistant-capture/*</files>
  <read_first>.planning/phases/03-assistant-capture/03-RESEARCH.md, zadachnik-app/src/lib/tasks/task-validation.ts</read_first>
  <action>Implement narrow Russian parsing for obvious date phrases (`сегодня`, `завтра`, `послезавтра`, `через неделю`, ISO dates), existing category names/default aliases, and importance phrases. Default natural dates to `doDate` unless the phrase clearly says deadline/sрок/крайний срок, then use `dueDate`. Leave ambiguous values empty or ask a clarification question instead of guessing silently.</action>
  <verify>`cd zadachnik-app && npm run typecheck`</verify>
  <acceptance_criteria>
    - Date parsing preserves Phase 2 `doDate` versus `dueDate`.
    - Category matching uses existing active categories and aliases only.
    - Importance maps to existing `normal` or `important`.
    - Unknown fields remain unset.
  </acceptance_criteria>
  <done>Parser covers Phase 3 interpretation targets.</done>
</task>

<task type="auto">
  <name>Task 3: Add protected interpretation API</name>
  <files>zadachnik-app/src/app/api/assistant-capture/interpret/route.ts, zadachnik-app/src/lib/tasks/task-queries.ts</files>
  <read_first>zadachnik-app/src/lib/api-session.ts, zadachnik-app/src/lib/tasks/task-queries.ts, zadachnik-app/src/app/api/tasks/route.ts</read_first>
  <action>Add `POST /api/assistant-capture/interpret`. It must require the existing API session, read the phrase, load active organization options, call the interpreter, and return the staged interpretation. Do not save a task from this route.</action>
  <verify>`cd zadachnik-app && npm run typecheck`</verify>
  <acceptance_criteria>
    - Unauthorized calls are rejected through the existing session helper.
    - Successful calls return interpreted task data and clarification questions.
    - No task rows are created by interpretation.
  </acceptance_criteria>
  <done>Protected interpretation endpoint is available to the quick capture UI.</done>
</task>

<task type="auto">
  <name>Task 4: Add parser-focused tests</name>
  <files>zadachnik-app/tests/assistant-capture-parser.spec.ts, zadachnik-app/tests/helpers/access.ts</files>
  <read_first>zadachnik-app/tests/dashboard-capture.spec.ts, zadachnik-app/tests/helpers/access.ts</read_first>
  <action>Add tests covering date, deadline, category, importance, incomplete phrase, and the protected interpretation endpoint not creating tasks. Use existing access/test helpers and seeded organization defaults.</action>
  <verify>`cd zadachnik-app && npm run test:e2e -- assistant-capture-parser.spec.ts`</verify>
  <acceptance_criteria>
    - Parser tests cover every Phase 3 recognition target.
    - Test proves interpretation alone does not save a task.
    - Tests do not touch Telegram, Gmail, `.env`, or session files.
  </acceptance_criteria>
  <done>Assistant interpretation contract is covered by tests.</done>
</task>

</tasks>

<verification>
- [ ] `cd zadachnik-app && npm run typecheck`
- [ ] `cd zadachnik-app && npm run test:e2e -- assistant-capture-parser.spec.ts`
</verification>

<success_criteria>
- User phrases can be interpreted into staged task data.
- Interpreter covers date, category, and importance without broad AI scope.
- Interpretation never saves a task before confirmation.
- All interpretation APIs respect the existing private-link session boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/03-assistant-capture/03-01-SUMMARY.md`.
</output>
