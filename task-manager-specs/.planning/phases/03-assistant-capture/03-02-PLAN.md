---
phase: 03-assistant-capture
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - zadachnik-app/src/features/capture/QuickCapturePanel.tsx
  - zadachnik-app/src/features/capture/*
  - zadachnik-app/src/app/globals.css
  - zadachnik-app/tests/assistant-capture-flow.spec.ts
autonomous: true
requirements: [CAPT-01, CAPT-02, CAPT-03, CAPT-04, CAPT-05]
user_setup: []
must_haves:
  truths:
    - "D-01/D-02/D-03: Assistant capture lives inside `Быстро`, preserves title-only quick capture, and remains compact/mobile-first with Russian UI."
    - "D-04/D-05/D-06: UI asks at most one or two clarification questions and then prepares a partial task card."
    - "D-07/D-08/D-09/D-10/D-11: UI shows an interpreted task card before saving, supports `Сохранить`, `Изменить`, and `Отмена`, and permits explicit save with missing optional fields."
    - "D-12/D-13/D-14/D-15/D-16: UI visibly presents interpreted date, category, and importance, and marks missing optional fields clearly."
  artifacts:
    - path: "zadachnik-app/src/features/capture/QuickCapturePanel.tsx"
      provides: "Assistant capture UI inside existing quick capture panel"
      contains: "Сохранить"
    - path: "zadachnik-app/src/app/globals.css"
      provides: "Mobile-first styling for assistant staged card and clarification prompts"
      contains: "capture-assistant"
    - path: "zadachnik-app/tests/assistant-capture-flow.spec.ts"
      provides: "Browser coverage for assistant clarification, confirmation, edit, and cancel flows"
      contains: "Быстро"
  key_links:
    - from: "zadachnik-app/src/features/capture/QuickCapturePanel.tsx"
      to: "zadachnik-app/src/app/api/assistant-capture/interpret/route.ts"
      via: "panel calls interpretation before showing confirmation"
      pattern: "/api/assistant-capture/interpret"
---

<objective>
Build the assistant-guided capture experience inside the existing `Быстро` panel.

Purpose: let the user enter a natural phrase, answer a short clarification when needed, inspect the interpreted task card, and choose save/edit/cancel before anything is created.
Output: mobile-first quick capture UI with manual and assistant paths, staged interpretation card, clarification handling, and flow tests.
</objective>

<execution_context>
@C:/Users/maksd/OneDrive/Документы/New project 2/task-manager-specs/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/OneDrive/Документы/New project 2/task-manager-specs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-assistant-capture/03-CONTEXT.md
@.planning/phases/03-assistant-capture/03-RESEARCH.md
@.planning/phases/03-assistant-capture/03-01-SUMMARY.md
@zadachnik-app/src/features/capture/QuickCaptureEntry.tsx
@zadachnik-app/src/features/capture/QuickCapturePanel.tsx
@zadachnik-app/src/app/globals.css
@zadachnik-app/tests/dashboard-capture.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add assistant mode inside quick capture</name>
  <files>zadachnik-app/src/features/capture/QuickCapturePanel.tsx, zadachnik-app/src/features/capture/*</files>
  <read_first>.planning/phases/03-assistant-capture/03-CONTEXT.md, zadachnik-app/src/features/capture/QuickCapturePanel.tsx</read_first>
  <action>Extend the existing panel with a compact assistant path while preserving the current title-only manual create path. Keep everything inside the existing `Быстро` dialog and use Russian labels only. The user should be able to type a phrase for assistant interpretation without leaving the panel.</action>
  <verify>`cd zadachnik-app && npm run typecheck`</verify>
  <acceptance_criteria>
    - Existing quick title-only capture still works.
    - Assistant capture starts from the same `Быстро` button.
    - No new main navigation item or standalone assistant page is added.
  </acceptance_criteria>
  <done>Quick capture panel contains both manual and assistant capture paths.</done>
</task>

<task type="auto">
  <name>Task 2: Show clarification and staged task interpretation</name>
  <files>zadachnik-app/src/features/capture/QuickCapturePanel.tsx</files>
  <read_first>.planning/phases/03-assistant-capture/03-RESEARCH.md, zadachnik-app/src/lib/assistant-capture/*</read_first>
  <action>Call the interpretation endpoint, render up to two clarification questions when returned, and show a compact interpreted task card with title, date, category, importance, and missing fields. The UI must make it obvious that the task has not been saved yet.</action>
  <verify>`cd zadachnik-app && npm run typecheck`</verify>
  <acceptance_criteria>
    - The first phrase alone does not save a task.
    - Clarification prompts are limited to two.
    - The staged card clearly shows interpreted values before save.
  </acceptance_criteria>
  <done>Assistant interpretation is visible before confirmation.</done>
</task>

<task type="auto">
  <name>Task 3: Add save, edit, and cancel confirmation controls</name>
  <files>zadachnik-app/src/features/capture/QuickCapturePanel.tsx</files>
  <read_first>.planning/phases/03-assistant-capture/03-CONTEXT.md, zadachnik-app/src/features/tasks/TaskForm.tsx</read_first>
  <action>Add explicit confirmation actions. `Сохранить` should save the staged task, `Изменить` should expose editable staged fields inside the panel before saving, and `Отмена` should discard staged interpretation without creating a task. Incomplete staged tasks may be saved if the user explicitly clicks `Сохранить`.</action>
  <verify>`cd zadachnik-app && npm run typecheck`</verify>
  <acceptance_criteria>
    - Save is explicit and visible.
    - Edit lets the user adjust at least title, date, category, and importance before save.
    - Cancel creates no task.
  </acceptance_criteria>
  <done>Confirmation contract is implemented in the panel UI.</done>
</task>

<task type="auto">
  <name>Task 4: Style the assistant capture state</name>
  <files>zadachnik-app/src/app/globals.css</files>
  <read_first>zadachnik-app/src/app/globals.css, .planning/phases/01-personal-app-shell/01-CONTEXT.md</read_first>
  <action>Add restrained mobile-first styles for assistant mode, clarification prompts, interpreted field rows, missing-field hints, and action buttons. Keep 8px radius patterns, stable input/button dimensions, and no marketing-style hero treatment.</action>
  <verify>`cd zadachnik-app && npm run typecheck`</verify>
  <acceptance_criteria>
    - Assistant card fits on mobile without overlapping text.
    - Buttons remain at least 44px high.
    - Styling stays consistent with the existing task UI.
  </acceptance_criteria>
  <done>Assistant capture UI is visually integrated with the app.</done>
</task>

<task type="auto">
  <name>Task 5: Cover assistant capture UI behavior</name>
  <files>zadachnik-app/tests/assistant-capture-flow.spec.ts, zadachnik-app/tests/dashboard-capture.spec.ts</files>
  <read_first>zadachnik-app/tests/dashboard-capture.spec.ts, zadachnik-app/tests/helpers/access.ts</read_first>
  <action>Add Playwright coverage for starting assistant capture from `Быстро`, seeing an interpretation before save, answering clarification, editing staged fields, and cancelling without creating a task. Keep or update existing quick-capture tests so manual capture remains protected.</action>
  <verify>`cd zadachnik-app && npm run test:e2e -- assistant-capture-flow.spec.ts dashboard-capture.spec.ts`</verify>
  <acceptance_criteria>
    - Test proves no task is created until explicit save.
    - Test proves cancel creates no task.
    - Test proves title-only capture still works.
  </acceptance_criteria>
  <done>Assistant capture UI behavior is covered by E2E tests.</done>
</task>

</tasks>

<verification>
- [ ] `cd zadachnik-app && npm run typecheck`
- [ ] `cd zadachnik-app && npm run test:e2e -- assistant-capture-flow.spec.ts dashboard-capture.spec.ts`
</verification>

<success_criteria>
- Assistant capture is available from `Быстро`.
- Manual quick capture remains fast and working.
- User sees the interpreted task before save.
- User can save, edit, or cancel the staged task.
- UI remains Russian-only and mobile-first.
</success_criteria>

<output>
After completion, create `.planning/phases/03-assistant-capture/03-02-SUMMARY.md`.
</output>
