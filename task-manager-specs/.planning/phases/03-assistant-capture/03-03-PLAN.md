---
phase: 03-assistant-capture
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - zadachnik-app/src/app/api/tasks/route.ts
  - zadachnik-app/src/lib/tasks/task-validation.ts
  - zadachnik-app/src/features/capture/QuickCapturePanel.tsx
  - zadachnik-app/tests/assistant-capture-save.spec.ts
  - zadachnik-app/tests/assistant-capture-mobile.spec.ts
autonomous: true
requirements: [CAPT-01, CAPT-02, CAPT-03, CAPT-04, CAPT-05]
user_setup: []
must_haves:
  truths:
    - "D-07/D-10/D-17: Confirmed assistant-created tasks save through the same task model and tables as manual tasks."
    - "D-08/D-16: Explicitly confirmed incomplete tasks may be saved with unknown optional fields empty."
    - "D-18: Any assistant source metadata remains lightweight and does not add Telegram/Gmail source handling."
    - "D-19: All save and interpretation operations remain protected by the existing private-link session boundary."
    - "D-01/D-02/D-03/D-04/D-05/D-06/D-09/D-11/D-12/D-13/D-14/D-15: End-to-end verification covers the full assistant capture contract from `Быстро` through confirmed save."
  artifacts:
    - path: "zadachnik-app/src/app/api/tasks/route.ts"
      provides: "Confirmed assistant-created task persistence through existing task API"
      contains: "categoryId"
    - path: "zadachnik-app/tests/assistant-capture-save.spec.ts"
      provides: "End-to-end save/cancel/no-premature-save coverage"
      contains: "Сохранить"
    - path: "zadachnik-app/tests/assistant-capture-mobile.spec.ts"
      provides: "Mobile viewport coverage for the assistant capture flow"
      contains: "Быстро"
  key_links:
    - from: "zadachnik-app/src/features/capture/QuickCapturePanel.tsx"
      to: "zadachnik-app/src/app/api/tasks/route.ts"
      via: "confirmed staged payload is persisted only after explicit save"
      pattern: "Сохранить"
---

<objective>
Wire confirmed assistant-created tasks into the existing core task system and verify the complete Phase 3 flow.

Purpose: ensure assistant capture produces normal tasks only after explicit confirmation and that Phase 3 requirements are covered on desktop and mobile.
Output: task creation API support for structured confirmed payloads, final UI integration, and full automated coverage.
</objective>

<execution_context>
@C:/Users/maksd/OneDrive/Документы/New project 2/task-manager-specs/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/maksd/OneDrive/Документы/New project 2/task-manager-specs/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-assistant-capture/03-CONTEXT.md
@.planning/phases/03-assistant-capture/03-RESEARCH.md
@.planning/phases/03-assistant-capture/03-01-SUMMARY.md
@.planning/phases/03-assistant-capture/03-02-SUMMARY.md
@zadachnik-app/src/app/api/tasks/route.ts
@zadachnik-app/src/app/api/tasks/[id]/route.ts
@zadachnik-app/src/lib/tasks/task-validation.ts
@zadachnik-app/src/features/capture/QuickCapturePanel.tsx
@zadachnik-app/tests/helpers/access.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Allow confirmed structured task creation</name>
  <files>zadachnik-app/src/app/api/tasks/route.ts, zadachnik-app/src/lib/tasks/task-validation.ts</files>
  <read_first>zadachnik-app/src/app/api/tasks/route.ts, zadachnik-app/src/app/api/tasks/[id]/route.ts, zadachnik-app/src/lib/tasks/task-validation.ts</read_first>
  <action>Extend task creation so confirmed assistant payloads can include the same safe optional fields used by Phase 2 edits: doDate, dueDate, categoryId, importance, and other already-supported simple fields if needed. Keep title required and keep unknown optional fields empty. Do not require a project, due date, category, or importance beyond defaults.</action>
  <verify>`cd zadachnik-app && npm run typecheck`</verify>
  <acceptance_criteria>
    - Existing title-only POST `/api/tasks` still works.
    - Confirmed assistant payload can create a task with date, category, and importance.
    - Invalid payloads return existing structured API errors.
  </acceptance_criteria>
  <done>Task creation API supports confirmed assistant payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Save staged assistant tasks only after confirmation</name>
  <files>zadachnik-app/src/features/capture/QuickCapturePanel.tsx</files>
  <read_first>zadachnik-app/src/features/capture/QuickCapturePanel.tsx, .planning/phases/03-assistant-capture/03-02-SUMMARY.md</read_first>
  <action>Connect the assistant confirmation card to the task creation API. The panel must create the task only when the user clicks `Сохранить`, then reset staged state, show a success message, and refresh the app. Interpretation, clarification, and edit steps must not create task rows.</action>
  <verify>`cd zadachnik-app && npm run typecheck`</verify>
  <acceptance_criteria>
    - Clicking `Сохранить` persists a normal task.
    - Interpretation and clarification steps create no tasks.
    - Success state matches the existing quiet capture behavior.
  </acceptance_criteria>
  <done>Confirmed assistant save is wired end to end.</done>
</task>

<task type="auto">
  <name>Task 3: Add final desktop and mobile E2E coverage</name>
  <files>zadachnik-app/tests/assistant-capture-save.spec.ts, zadachnik-app/tests/assistant-capture-mobile.spec.ts</files>
  <read_first>zadachnik-app/tests/dashboard-capture.spec.ts, zadachnik-app/tests/organization.spec.ts, zadachnik-app/tests/helpers/access.ts</read_first>
  <action>Add end-to-end tests proving the full Phase 3 contract: assistant starts from `Быстро`, first phrase does not save, interpretation/clarification appears, confirmed save creates a normal task with date/category/importance, incomplete confirmed save is allowed, cancel creates no task, and the mobile viewport renders the flow without overlap.</action>
  <verify>`cd zadachnik-app && npm run test:e2e -- assistant-capture-save.spec.ts assistant-capture-mobile.spec.ts`</verify>
  <acceptance_criteria>
    - Tests cover CAPT-01 through CAPT-05.
    - Tests cover no premature save.
    - Tests cover incomplete confirmed save.
    - Tests cover mobile usability.
  </acceptance_criteria>
  <done>Full assistant capture flow is covered on desktop and mobile.</done>
</task>

<task type="auto">
  <name>Task 4: Run full Phase 3 verification</name>
  <files>zadachnik-app/tests/*, zadachnik-app/src/*</files>
  <read_first>.planning/phases/03-assistant-capture/03-CONTEXT.md, .planning/phases/03-assistant-capture/03-RESEARCH.md</read_first>
  <action>Run the final verification suite for Phase 3. Fix narrow regressions caused by assistant capture while preserving Phase 2 task behavior. Do not start Phase 4 dashboard ranking or Phase 8 communication intake.</action>
  <verify>`cd zadachnik-app && npm run typecheck && npm run build && npm run test:e2e`</verify>
  <acceptance_criteria>
    - TypeScript passes.
    - Production build passes with the local SQLite database URL.
    - Playwright E2E passes.
    - Manual quick capture still works.
  </acceptance_criteria>
  <done>Phase 3 is ready for human verification.</done>
</task>

</tasks>

<verification>
- [ ] `cd zadachnik-app && npm run typecheck`
- [ ] `cd zadachnik-app && DATABASE_URL=file:./dev.db npm run build`
- [ ] `cd zadachnik-app && DATABASE_URL=file:./dev.db npm run test:e2e`
</verification>

<success_criteria>
- User can start assistant-style task creation through `Быстро`.
- Assistant asks for missing details or shows a task-card interpretation.
- Assistant never saves after the first phrase without confirmation.
- Confirmed assistant-created tasks use the same task model as manual tasks.
- Date, category, and importance interpretation are visible and test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/03-assistant-capture/03-03-SUMMARY.md` and prepare Phase 3 for user verification.
</output>
